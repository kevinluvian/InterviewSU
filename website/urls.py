from django.conf.urls import url
import website.views


urlpatterns = [
    # url(r'^login/',interviewee.views.LoginViewSet.as_view()),
    # url(r'^queue/',interviewee.views.Test.as_view())
    # url(r'^interviewer/',interviewee.views.Test.as_view())
    # url(r'^boss/',interviewee.views.Test.as_view())
]